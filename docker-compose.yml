services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: ttm
      POSTGRES_PASSWORD: ttm
      POSTGRES_DB: ttm_tracker
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
